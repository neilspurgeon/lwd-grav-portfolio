<nav class="main-nav-container">
  <a class="left-nav" href="{{ base_url }}">
    <h1 class="site-title">{{ site.title }}</h1>
  </a>

  <ul class="right-nav">
    {% for p in pages.children %}
    <li>
      <a href="{{p.url}}">
        <h2>{{ p.title }}</h2>
      </a>
    </li>
    {% endfor %}
  </ul>
</nav>